<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jam.file.dao.ImageFileDAO">

	<insert id="insertImage" parameterType="imageFile">
	    INSERT INTO image_file (image_id, image_name, post_id, post_type, created_at)
	    VALUES (seq_image_file.NEXTVAL, #{image_name}, #{post_id}, #{post_type}, SYSDATE)
	</insert>
	
	<select id="findNameById" parameterType="Long" resultType="String">
		SELECT image_name
		FROM image_file
		WHERE image_id = #{image_id}
	</select>
	
	<delete id="deleteImage" parameterType="Long">
		DELETE FROM image_file
		WHERE image_id = #{image_id} 
	</delete>
</mapper>
