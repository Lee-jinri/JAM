name: Deploy to EC2

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout source
      uses: actions/checkout@v4

    - name: Create application.yaml
      run: |
        cat > src/main/resources/application.yaml << 'EOF'
        spring:
          datasource:
            url: ${{ secrets.DB_URL }}
            username: ${{ secrets.DB_USERNAME }}
            password: ${{ secrets.DB_PASSWORD }}
          redis:
            host: ${{ secrets.REDIS_HOST }}
            port: ${{ secrets.REDIS_PORT }}
            database: 0
          redisson:
            address: ${{ secrets.REDISSON_ADDRESS }}
          mail:
            username: ${{ secrets.MAIL_USERNAME }}
            password: ${{ secrets.MAIL_PASSWORD }}
            
        server:
          servlet:
            encoding:
              force: true
              force-response: true
              charset: UTF-8
              
        jwt:
          secret: ${{ secrets.JWT_SECRET }}
          
        oauth:
          naver:
            clientId: ${{ secrets.NAVER_CLIENT_ID }}
            clientSecret: ${{ secrets.NAVER_CLIENT_SECRET }}
            redirectUri: ${{ secrets.NAVER_REDIRECT_URI }}
          kakao: 
            clientId: ${{ secrets.KAKAO_CLIENT_ID }}
            redirectUri: ${{ secrets.KAKAO_REDIRECT_URI }}
            
        location:
          consumer_key: ${{ secrets.LOCATION_CONSUMER_KEY }}
          consumer_secret: ${{ secrets.LOCATION_CONSUMER_SECRET }}
          save:
            path: ${{ secrets.LOCATION_SAVE_PATH }}

        cloud:
          aws:
            s3:
              bucket: jam-resume-bucket

        file:
          upload-dir: ${{ secrets.FILE_UPLOAD_DIR }}
        
        cors: 
          allowed-origins: ${{ secrets.CORS_ALLOWED_ORIGINS }}
        EOF  

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        distribution: 'corretto'
        java-version: '17'

    - name: Build with Maven
      run: mvn -B package -Dmaven.test.skip=true
      working-directory: ./

    - name: List WAR files
      run: ls -lh ./target

    - name: Debug WAR path
      run: |
        echo "SEARCHING:"
        find -name "*.war" -type f

    - name: Copy WAR file with native scp
      env:
        EC2_KEY: ${{ secrets.EC2_KEY }}
      run: |
        echo "$EC2_KEY" > /tmp/ec2_key.pem
        chmod 400 /tmp/ec2_key.pem
    
        WAR_FILE=$(find "./target" -name "*.war" -type f | head -n 1)
        if [ -z "$WAR_FILE" ]; then
          echo "ERROR: WAR 파일을 찾을 수 없습니다"
          exit 1
        fi
    
        echo "배포할 WAR 파일: $WAR_FILE"
        ls -lh "$WAR_FILE"
    
        scp -v -o StrictHostKeyChecking=no -i /tmp/ec2_key.pem \
          "$WAR_FILE" \
          ec2-user@${{ secrets.EC2_HOST }}:/home/ec2-user/tomcat/webapps/ROOT.war
    
        rm -f /tmp/ec2_key.pem
    
    
    - name: Restart Tomcat on EC2
      uses: appleboy/ssh-action@v0.1.8
      with:
        host: ${{ secrets.EC2_HOST }}
        username: ec2-user
        key: ${{ secrets.EC2_KEY }}
        script: |          
          echo "AWS 환경변수 설정..."
          export AWS_ACCESS_KEY_ID=${{ secrets.AWS_ACCESS_KEY_ID }}
          export AWS_SECRET_ACCESS_KEY=${{ secrets.AWS_SECRET_ACCESS_KEY }}
          export AWS_DEFAULT_REGION=${{ secrets.AWS_DEFAULT_REGION }}
          
          echo "기존 Tomcat 종료 중..."
          sudo fuser -k 8080/tcp || true
          sleep 3
          
          echo "권한 복구..."
          sudo chown -R ec2-user:ec2-user /home/ec2-user/tomcat
          
          echo "Tomcat 재시작..."
          export JAVA_HOME=/usr/lib/jvm/java-17-amazon-corretto.x86_64
          echo "===== DEPLOY $(date '+%Y-%m-%d %H:%M:%S %Z') =====" >> /home/ec2-user/tomcat/logs/catalina.out
          setsid /home/ec2-user/tomcat/bin/catalina.sh start >> /home/ec2-user/tomcat/logs/catalina.out 2>&1 < /dev/null &

          echo "배포 완료. 5초 후 로그 확인 시작..."
          sleep 5
          /usr/bin/tail -n 50 /home/ec2-user/tomcat/logs/catalina.out
